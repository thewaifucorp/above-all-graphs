name: ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - run: cargo fmt --check
      - run: cargo clippy --all-targets --all-features -- -D warnings
      - run: cargo test
      # The npm wrapper is how most users get the binary, and its asset
      # selection is the difference between installing embeddings and
      # silently not installing them.
      # Unquoted so the shell expands it. Node 24 stopped expanding the
      # pattern itself, and a quoted glob became "Could not find npm/*.test.js".
      - run: node --test npm/*.test.js
