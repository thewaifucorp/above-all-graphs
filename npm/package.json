{
  "name": "@waifucorp/aag",
  "version": "0.1.0",
  "description": "AboveAllGraphs — code knowledge graph that installs itself, stays fresh, and works with every coding agent. This package only downloads the prebuilt Rust binary for your platform; nothing is compiled.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/waifucorp/above-all-graphs.git"
  },
  "keywords": ["mcp", "code-graph", "knowledge-graph", "tree-sitter", "ai-agents"],
  "bin": {
    "aag": "bin/aag.js"
  },
  "scripts": {
    "postinstall": "node install.js"
  },
  "files": ["bin/aag.js", "install.js"],
  "os": ["linux", "darwin", "win32"],
  "cpu": ["x64", "arm64"],
  "engines": {
    "node": ">=18"
  }
}
